# ╔══════════════════════════════════════════╗
# ║        SilpoMarket — requirements.txt    ║
# ╚══════════════════════════════════════════╝
#
# Встановлення всіх залежностей одною командою:
#   pip install -r requirements.txt
#
# Перевірена версія Python: 3.8+
# ─────────────────────────────────────────────

# GUI фреймворк — сучасний вигляд, підтримка темної/світлої теми
customtkinter>=5.2.0

# Обробка зображень — завантаження та масштабування фото товарів
Pillow>=9.0.0

# Числові обчислення — алгоритм Flood Fill для прозорості логотипу
numpy>=1.21.0

# ─────────────────────────────────────────────
# Вбудовані в Python (встановлювати НЕ потрібно):
#   tkinter      — базовий GUI
#   sqlite3      — база даних
#   threading    — фонове завантаження фото
#   urllib       — HTTP запити для фото
#   hashlib      — SHA-256 хешування паролів
#   datetime     — дата і час замовлень
#   io, os, sys  — системні утиліти
# ─────────────────────────────────────────────
